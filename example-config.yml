# Example configuration file for gh-repo-manager
# Place this file at ~/.config/gh-repo-man.yml or use --config flag to specify a custom path

# Show README content in the fzf preview pane
# Default: false
show_readme_in_preview: true

# Cache time-to-live settings
# Supported units: m (minutes), h (hours), d (days)

# How long to cache repository lists
# Default: 24h
repos_cache_ttl: 24h

# How long to cache README files  
# Default: 24h
readme_cache_ttl: 24h

# How long to cache usernames
# Default: 90d
username_cache_ttl: 90d

# Directory where repositories will be cloned
# Supports ~ expansion for home directory
# Default: ~/Projects
projects_dir: ~/Projects

# Organize repositories by username in subdirectories
# When enabled: ~/Projects/username/repo-name
# When disabled: ~/Projects/repo-name
# Default: true
per_user_dir: true

# Enable tea integration for tmux session management
# When enabled and tea is available, opens cloned repos with tea
# Default: true
tea_integration: true

# Editor to use when tea is not available or disabled
# Falls back to $EDITOR environment variable, then nvim
# Default: $EDITOR or nvim
editor: nvim
